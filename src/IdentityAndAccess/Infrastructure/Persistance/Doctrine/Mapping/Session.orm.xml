<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping
    xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                        https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\IdentityAndAccess\Domain\Entity\Session"
            table="sessions"
            repository-class="App\IdentityAndAccess\Domain\Repository\SessionRepository">

        <!-- ID UUID -->
        <id name="id" column="id" type="string"/>

        <field name="userId" column="user_id" type="string" />

        <!-- Tracking -->
        <field name="ipAddress" column="ip_address" type="string" length="45" nullable="true" />
        <field name="userAgent" column="user_agent" type="text" nullable="true" />

        <!-- Dates -->
        <field name="createdAt" column="created_at" type="datetime_immutable" />

        <!-- Index -->
        <indexes>
            <index name="idx_session_user_id" columns="user_id"/>
            <index name="idx_session_created_at" columns="created_at"/>
        </indexes>

    </entity>
</doctrine-mapping>
